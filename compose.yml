services:
  database:
    image: postgres:15.5
    container_name: database
    environment:
      - POSTGRES_DB=blogy
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - 5432:5432